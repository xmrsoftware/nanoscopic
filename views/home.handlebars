<h1>{{title}}</h1>
{{#if blog_posts}}
    {{#each blog_posts}}
        <h2>{{blog_post_header}}</h2>
        <p>
            {{blog_post_free_content}}
        </p>
        <p>
            <a href="/post/show/{{blog_user_id}}/{{blog_post_id}}/{{blog_post_url_slug}}/">Read more</a>.
        </p>
    {{/each}}
{{else}}
    <p>
        There are no blog posts posted yet.
    </p>
{{/if}}